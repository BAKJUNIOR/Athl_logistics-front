// Entité métier "Example" : représente l'objet métier et sa logique propre (ex: isValid(), calculs...).
// Ce fichier est un modèle à dupliquer/adapter pour chaque nouveau domaine (ex: shipment.entity.ts, fleet.entity.ts).
